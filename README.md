# Packing checklist

Built output only — the generator and the source list live in a private repo.

Open <https://narsino.github.io/packing/> on a phone, then **Share → Add to Home Screen**. The service
worker caches the page on first load, so it works afterwards with no signal.

Not indexed (`robots.txt`, `noindex`), but this repo is public: treat anything
here as readable by anyone with the URL.
